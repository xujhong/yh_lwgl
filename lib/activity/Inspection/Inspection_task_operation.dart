import 'package:flutter/material.dart';

//巡检任务操作==》通过/纠正
class InspectionTaskOperation extends StatefulWidget {
  @override
  _inspectionTaskOperation createState() => _inspectionTaskOperation();
}

class _inspectionTaskOperation extends State<InspectionTaskOperation> {
  @override
  Widget build(BuildContext context) {
    // TODO: implement build
    return Scaffold(
      appBar: AppBar(
        title: Text('通过'),
      ),
      body: Container(
        child: SingleChildScrollView(
          child: Column(

          ),
        ),
      ),
    );
  }
}
